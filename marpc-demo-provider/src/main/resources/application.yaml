spring:
  application:
    name: marpc-demo-provider

server:
  port: 8081

marpc:
  zk:
    address: localhost:2181
  app: marpc-app
  env: dev
  provider:
    instance: localhost:8080
  loadbalancer: roundrobin
